def pascal_case(sentence: str) -> str:
    """
    Convertit une phrase en PascalCase (CamelCase avec première lettre majuscule).
    Pas de boucle et ne pas muter la chaîne originale.
    
    Ton code en dessous
    """




"""
Lancer le test : 
Dans le terminal se positioner dans le dossier src
Utiliser la commande : pytest tests/list_functions/test_pascal_case.py -q

Aperçu du test : 

def test_pascal_case_basic():
    assert pascal_case("this is sparta") == "ThisIsSparta"
    assert pascal_case("sO rAdicAL DuDe") == "SoRadicalDude"

def test_pascal_case_no_mutation():
    original = "no mutation"
    result = pascal_case(original)
    assert original == "no mutation"
    assert result == "NoMutation"
"""