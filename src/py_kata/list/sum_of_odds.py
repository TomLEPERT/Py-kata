from typing import Iterable

def sum_of_odds(values: Iterable[int]) -> int:
    """
    Somme de tous les nombres impairs dans `values`.
    
    Ton code en dessous
    """



"""
Lancer le test : 
Dans le terminal se positioner dans le dossier src
Utiliser la commande : pytest tests/list/test_sum_of_odds.py -q

Aperçu du test : 

def test_sum_of_odds_basic():
    assert sum_of_odds([1,2,3,4,5]) == 1+3+5
    assert sum_of_odds([]) == 0
    assert sum_of_odds([2,4,6]) == 0
    assert sum_of_odds([-3, -2, -1, 0, 1]) == -3 + -1 + 1
"""